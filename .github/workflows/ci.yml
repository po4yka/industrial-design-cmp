name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v6

      - name: Assemble library
        run: ./gradlew :library:assemble :library:compileKotlinMetadata --stacktrace --no-daemon

      - name: Assemble demo
        run: ./gradlew :demo:assemble --stacktrace --no-daemon
        continue-on-error: true

      - name: Run library tests
        run: ./gradlew :library:allTests --stacktrace --no-daemon
        continue-on-error: true

      - name: Run ktlint and detekt
        run: ./gradlew ktlintCheck detekt --stacktrace --no-daemon
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: "**/build/test-results/**"
          if-no-files-found: ignore

  ios-check:
    runs-on: macos-14
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v6

      - name: Link iOS simulator framework
        run: ./gradlew :library:linkDebugFrameworkIosSimulatorArm64 --stacktrace --no-daemon
